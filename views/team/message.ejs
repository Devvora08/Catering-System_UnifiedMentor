<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Team | Messages</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #0b0010; /* Very dark greyish-blue background */
            color: #f0f0f0; /* Light gray text for better visibility */
            margin: 0;
            padding: 20px;
        }
        h1 {
            text-align: center;
            font-size: 2.5em;
            margin-bottom: 20px;
            color: #d0d0d0; /* Light color for heading */
            font-weight: bold;
        }
        .message-container {
            border: 1px solid #130536; /* Dark purple border for boxes */
            border-radius: 8px;
            padding: 15px;
            background-color: #2a2a2a53; /* Dark background for messages */
            transition: transform 0.3s ease-in-out, opacity 0.3s ease-in-out;
            opacity: 0; /* Start with hidden */
            transform: translateY(20px); /* Start slightly below */
            flex: 1 1 calc(30% - 20px); /* Allow three boxes per row with some margin */
            margin: 10px; /* Margin for spacing between boxes */
        }
        .message-container.visible {
            opacity: 1; /* Make visible */
            transform: translateY(0); /* Move to original position */
        }
        .message-header {
            font-weight: bold;
            margin-bottom: 10px;
            color: #e0e0e0; /* Slightly lighter for header */
        }
        .message-content {
            margin-bottom: 5px;
            color: #f0f0f0; /* Light gray content */
        }
        .message-time {
            font-size: 0.9em;
            color: #aaa; /* Light gray for time */
        }
        .back-button {
            display: block;
            margin: 20px auto;
            padding: 10px 15px;
            width: 20%;
            background-color: #3D2A55; /* Much darker purple button */
            color: white;
            text-align: center;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.3s;
        }
        .back-button:hover {
            background-color: #2B1D3A; /* Darker purple on hover */
        }
        .messages-wrapper {
            display: flex; /* Use flexbox for layout */
            flex-wrap: wrap; /* Allow wrapping of boxes */
            justify-content: center; /* Center the boxes */
            margin: 0 auto; /* Center the wrapper */
            max-width: 900px; /* Limit maximum width */
        }
    </style>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const messageContainers = document.querySelectorAll('.message-container');
            messageContainers.forEach((container, index) => {
                setTimeout(() => {
                    container.classList.add('visible');
                }, index * 200); // Staggered effect
            });
        });
    </script>
</head>
<body>
    <h1>Team Messages</h1>

    <div class="messages-wrapper">
        <% if (messages.length > 0) { %>
            <% messages.forEach(message => { %>
                <div class="message-container">
                    <div class="message-header"><%= message.senderRole %>:</div>
                    <div class="message-content"><%= message.content %></div>
                    <div class="message-time"><%= message.createdAt.toLocaleString() %></div>
                </div>
            <% }) %>
        <% } else { %>
            <p>No messages available.</p>
        <% } %>
    </div>
    
    <a href="/team/home" class="back-button">Back to Home</a> <!-- Link to navigate back to team home -->
</body>
</html>
