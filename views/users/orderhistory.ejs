<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order History</title>
</head>

<body>
    <h1>Your Order History</h1>

    <% if (orders.length > 0) { %>
        <% orders.forEach(order => { %>
            <div class="order">
                <h2>Order ID: <%= order._id %></h2>
                <p><strong>Status:</strong> <%= order.status %></p>
                <p><strong>Date:</strong> <%= order.createdAt.toDateString() %></p>
                <h3>User Details</h3>
                <p><strong>Name:</strong> <%= user.name %></p>
                <p><strong>Email:</strong> <%= user.email %></p>
                <p><strong>Contact:</strong> <%= user.phone %></p>
                <p><strong>Address:</strong> <%= order.deliveryAddress.join(', ') %></p>

                <h3>Order Items</h3>
                <ul>
                    <% order.items.forEach(item => { %>
                        <li>
                            <%= item.foodItem.name %> - Quantity: <%= item.quantity %> - Price: <%= item.foodItem.price * item.quantity %> Rs
                        </li>
                    <% }) %>
                </ul>

                <h3>Total Amount: <%= order.totalBill %> Rs</h3>
                <p><strong>Special Instructions:</strong> <%= order.specialInstructions || 'None' %></p>
            </div>
            <hr>
        <% }) %>
    <% } else { %>
        <p>You have no orders yet.</p>
    <% } %>

    <a href="/user/cart">Go back to Cart</a>
</body>

</html>
