<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Send Message to Team</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .form-container {
            width: 100%;
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f9f9f9;
        }
        .form-container h2 {
            margin-top: 0;
            font-size: 1.5em;
            color: #333;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .form-group textarea {
            width: 100%;
            padding: 10px;
            font-size: 1em;
            border-radius: 4px;
            border: 1px solid #ddd;
        }
        .form-group input[type="hidden"] {
            display: none;
        }
        .submit-btn {
            padding: 10px 20px;
            font-size: 1em;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .submit-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <div class="form-container">
        <h2>Send a Message to Team</h2>
        <form action="/admin/message" method="POST">
            <!-- Hidden field to set sender role as 'admin' -->
            <input type="hidden" name="senderRole" value="admin">
            <!-- Hidden field to set recipient role as 'team' -->
            <input type="hidden" name="recipientRole" value="team">

            <!-- Message Content -->
            <div class="form-group">
                <label for="content">Message Content</label>
                <textarea name="content" id="content" rows="5" placeholder="Enter your message here..." required></textarea>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="submit-btn">Send Message</button>
        </form>
    </div>
    <button onclick="window.location.href='/admin/seeMessages'">See Messages</button>

</body>
</html>
